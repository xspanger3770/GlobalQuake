package globalquake.ui.globe;

public record Animation(double initialLat, double initialLon, double initialScroll, double targetLat, double targetLon,
                        double targetScroll) {
}
